import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

import "leaflet/dist/leaflet.css";

export const metadata = {
  title: "EcoRoute - AQI-Aware Delivery Routing for Delhi NCR",
  description: "Logistics dispatch dashboard routing delivery riders between warehouses and Delhi NCR neighborhoods, treating winter AQI as a routing constraint alongside distance.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
